import { AppStoreManager } from './AppManager';

// Just mount all store managers here
export const StoreManagers = () => {
    return (
        <>
            <AppStoreManager />
        </>
    );
};
